name: Build roothide

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - main
    paths:
      - '.github/workflows/roothide-build.yml'
      - '**/*.h'
      - '**/*.m'
      - '**/*.xm'
      - '**/*.xmi'
      - '**/*.plist'
      - '**/*.inc'
      - 'Makefile'
      - 'control'
      - 'layout/**'

jobs:
  package:
    name: Package (roothide)
    runs-on: macos-14

    env:
      THEOS: ${{ github.workspace }}/theos
      FINALPACKAGE: 1

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

      - name: Install packaging tools
        run: |
          brew update
          brew install dpkg ldid make

      - name: Install Theos
        run: |
          git clone --recursive https://github.com/roothide/theos.git "$THEOS"

      - name: Install Theos SDK with private frameworks
        run: |
          git clone --depth 1 --filter=blob:none --sparse https://github.com/theos/sdks.git /tmp/theos-sdks
          git -C /tmp/theos-sdks sparse-checkout set iPhoneOS16.5.sdk
          mkdir -p "$THEOS/sdks"
          cp -R /tmp/theos-sdks/iPhoneOS16.5.sdk "$THEOS/sdks/"

      - name: List available schemes
        run: ls "$THEOS/makefiles/package/" 2>/dev/null || echo "No package dir"

      - name: Build package
        run: |
          export PATH="/opt/homebrew/opt/make/libexec/gnubin:$PATH"
          # List available schemes for debugging
          echo "Available schemes:"
          ls "$THEOS/makefiles/package/" 2>/dev/null || true
          make clean SDKVERSION=16.5
          make package FINALPACKAGE=1 SDKVERSION=16.5

      - name: Upload deb packages
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: promotion120-roothide-packages
          path: packages/*
          if-no-files-found: error
