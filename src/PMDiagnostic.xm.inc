// Diagnostic / scopeprobe / jank / fpsprobe paths removed in 1.0.9+clean2.
// Behavior code must not call PMProbe*/PMJank*/PMFPS* helpers.
// Restore from git history only if a temporary probe build is needed.
