# Doimty Repo

GitHub Pages hosted jailbreak APT repo.

Source URL:

```text
https://doimty.github.io/test/
```

## Add a deb

Put `.deb` files into `debs/`, then run:

```bash
./scripts/update-repo.sh
```

Commit and push generated `Packages*` and `Release` files.
